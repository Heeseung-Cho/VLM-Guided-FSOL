# Core deep learning stack
# torch / torchvision must match your local CUDA. The authors used the
# CUDA 12.8 wheels installed via `pip install torch==2.8.0 torchvision==0.23.0
# --index-url https://download.pytorch.org/whl/cu128`.
torch==2.8.0
torchvision==0.23.0

# Detection and segmentation
# mmcv 2.x must be installed via the OpenMMLab wheel index, not from PyPI:
#   pip install -U openmim
#   mim install "mmcv==2.1.0" "mmengine==0.10.7" "mmdet==3.3.0"
mmcv==2.1.0
mmengine==0.10.7
mmdet==3.3.0
segment-anything==1.0
pycocotools==2.0.11

# VLM (Qwen3-VL) backend
ms_swift==3.9.2
transformers==4.57.1
tokenizers==0.22.1
safetensors==0.5.3
accelerate==1.10.0
peft==0.14.0
qwen-vl-utils==0.0.14

# General utilities
einops==0.6.1
huggingface-hub==0.35.3
numpy==1.26.4
opencv-python==4.11.0.86
pandas==1.5.3
pillow==11.1.0
pyyaml==6.0.2
regex==2024.11.6
requests==2.32.4
scikit-learn==1.5.0
scipy==1.11.4
sentencepiece==0.2.0
tiktoken==0.9.0
timm==1.0.21
tqdm==4.67.1